using UnityEngine;
using System.Collections;

public class beamSpreadBuzzScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}

	/*
	  
	        m_fDistance = Vector3.Distance(m_shooterTransform.position, m_vTargetPos);
            beamSpreads.material.SetTextureOffset("_MainTex", new Vector2(Random.Range(0f, 1.0f), 0));
            beamSpreads.material.SetTextureScale("_MainTex", new Vector2(m_fDistance / 8f, 1f));

            // set beam origin to barrel's position
            laserbeam.SetPosition(0, m_shooterTransform.position);
            beamSpreads.SetPosition(0, m_shooterTransform.position);

	*/

	// Update is called once per frame
	void Update () {
	
	}
}
